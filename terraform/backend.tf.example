# Optional: Store Terraform state in S3 (recommended for production)
# Uncomment and configure after creating an S3 bucket for state

# terraform {
#   backend "s3" {
#     bucket = "your-terraform-state-bucket"
#     key    = "trial-issues-tracker/terraform.tfstate"
#     region = "us-east-1"
#     encrypt = true
#     
#     # Optional: Use DynamoDB for state locking
#     # dynamodb_table = "terraform-state-lock"
#   }
# }
